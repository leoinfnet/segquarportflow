spring:
  application:
    name: terminal-service
  mongodb:
    uri: mongodb://terminal_service:terminal_service@172.168.52.33:27017/terminal_service

server:
  port: 8080

eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

